name: My Workflow
on:
  pull_request_review:
    types: [submitted]
    branches:
      - main
  push:
    branches:
      - main
    paths:
      - 'dina.txt' 
jobs:
  my_job:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request_review' && github.event.review.state == 'approved'
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    - name: Run my task
      run: |
        echo "My task runs because a commit was made to a file in the folder and the pull request was approved."
        # Run your task here
