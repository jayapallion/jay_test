name: Pull Request Approve and File Change

on:
  pull_request:
    types: [approved]
    paths:
      - 'dina.txt'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2
